# Research verification

Implemented against `dev` at `8b63e011c71de0b8ef377965005a537d58710f69`, only in
`sndcds/uranus-admin`. The original working copy was preserved; implementation and
validation used an isolated clone. No source schema mutation or Uranus repository
change was performed.

## Automated checks

- Backend: `.venv/bin/ruff check .`, `.venv/bin/ruff format --check .`,
  `.venv/bin/mypy`, `.venv/bin/python -m pytest -q` from `backend/`, with a
  disposable PostgreSQL/PostGIS database: **2,002 passed, no skips**. The final
  source-link normalization fix also passes all seven focused cases.
- Frontend: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`: passed.
  Unit suite: **839 tests**. Five pre-existing lint warnings remain.
- Full production browser CI: `TEST_PRODUCTION=1 pnpm test:e2e`, **531 passed,
  41 existing matrix skips**, including Research. No new skipped assertion.
- Final Research journeys: `pnpm test:e2e tests/e2e/research.spec.ts --workers=2`,
  **16 passed** against development; the same Playwright suite with
  `TEST_PRODUCTION=1` in the pinned CI browser, **16 passed** against production.
- Ansible: Ruff check/format with `backend/pyproject.toml` and syntax checks for
  `deploy.yml`, `preflight.yml`, `sql-console-audit.yml`: passed.
  `python -m unittest discover -s ansible/tests -v` in the isolated controller
  environment: **299 tests, 66 DB-dependent skips**. Separate disposable database
  runs verify bootstrap/upgrades/artifact (**21 tests**), role boundaries
  (**7 tests**) and real console check/diff/apply/idempotency. The CI matrix runs
  PostgreSQL 16/17 and PostGIS variants.
- `git diff origin/dev...HEAD --check`: passed. OpenAPI structural equality,
  Operations regressions, strict response projections, grant revocation and SQL
  WebSocket authorization are covered by the suites above.

[Release-commit CI](https://github.com/sndcds/uranus-admin/actions/runs/36241465998)
checks the deployed application code. Commits through `8f53dc7` after that release
only improve test readiness and documentation. The subsequent visual refinement
is separate from this rollout.

The first host-browser full run had OS-dependent screenshot differences and
existing test timeouts. All five affected cases passed in the pinned browser
without relaxing assertions. Development Research tests explicitly wait for
client-loaded results/interactivity after SSR navigation. Existing skips are
reported, not treated as successful executions.

## Visual review

[Desktop/mobile screenshots](../frontend/docs/screenshots/research-workspace/README.md)
cover landing, split search, all three dossier types and mobile search/event
views. These use synthetic records and tiles. The second visual pass follows the
new supplied collection, landing and dossier mockups as the primary specification,
while retaining existing components, fonts, tokens and responsive behavior.

## Authorized LXD test rollout

On 2026-09-26, the existing Ansible test deployment controller rolled out application
commit `dd0865b85ae6898cbc7ccb792e825f9282788940`. Its existing build functions built
that explicit feature commit locally from committed sources, validated standalone
Nitro startup and packaged the release. The normal main-branch wrapper was not used
because this task explicitly targets the Research branch.

A new full custom-format database backup was created with restricted permissions,
its checksum recorded and its archive listing verified; no restore test is claimed.
After the successful full `--check --diff`, Apply finished with **182 OK, 28 changed,
0 failed**. Migration 0014 → 0015, separated runtime/operator grants, service health,
readiness, local frontend and public HTTPS login checks passed.

The existing source catalog verifier confirmed the test database contract without
missing tables/columns or type mismatches. After deployment, all three Research
collections, their first available dossier and filtered exports validated against
real source data in a READ ONLY / REPEATABLE READ transaction. Operator `doctor`
passed all grant checks. These are test-container results, not a production rollout
or proof of a complete historical source audit trail.

An initial dry run correctly refused extra Python bytecode generated by the manual
catalog check. The pinned toolchain was restored from its verified archive and
installer permission rules before retrying. No safety check was relaxed. Subsequent
operator/source checks used Python `-B`.

## Visual refinement follow-up

PR #131 was merged while the refinement was in progress. The existing
`feat/research-workspace` branch was fast-forwarded to `dev` at `1c012a9`; the
visual changes are subsequent commits on the same branch and are submitted as a
follow-up PR against `dev`. They are not part of the already merged PR.
No backend, auth, SQL, API contract, proxy, dependency or deployment changes are included.

The revised mockups guide search, map, event/venue/organization collections,
landing, dossiers and table presentation. Shared components remain in use.
The Research table adds keyboard preview selection through DenseTable's existing
action slot. The advanced-filter dialog discards cancelled edits; applying it
uses the existing URL filter contract. Tests also cover header search/history,
combined date-chip removal, clipboard actions, image layout and avoiding an event
photo in a derived venue popup.

- `pnpm lint`: passed, four pre-existing warnings, no errors.
- `pnpm typecheck`: passed.
- `pnpm test`: **854 passed**.
- `pnpm build`: passed locally, not on the deployment host.
- Full production suite during the visual pass, before the final Research-only
  table/filter/category refinements, in the pinned CI browser:
  `TEST_PRODUCTION=1 node node_modules/@playwright/test/cli.js test --workers=4`:
  **543 passed, 41 existing matrix skips**, including unchanged Operations visual
  baselines. No baseline outside Research was updated.

The final table, cancelled-filter and canonical-category refinements are checked
by `pnpm test:e2e tests/e2e/research.spec.ts --workers=2` (34 passed) and, after the
local build, `TEST_PRODUCTION=1 node node_modules/@playwright/test/cli.js test
tests/e2e/research.spec.ts --workers=2` in the pinned CI container (34 passed).

Category mappings are verified against the referenced public client's
`CategorySelector.vue` and `event.scss`; they are not inferred from fixture labels.
All six exact colors, neutral fallback, translated labels, rendering order,
results, preview, active filter and dossier/usage badges have regression coverage.
Status/entity colors are separate. White-on-color contrast was calculated and a
dark-text/light-background badge with the exact original-color dot was chosen.
Screenshots use the pinned CI browser, synthetic records and intercepted map tiles. Missing fixture photos stay missing;
a separate image fixture checks the image-bearing layout. The decorative landing
illustration is local SVG. Actual account names, unavailable clusters, source
history, saved research and mockup-only actions are not invented.

This visual follow-up has not been deployed to the LXD test host.
